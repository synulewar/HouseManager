<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        package="com.synowkrz.housemanager">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
            android:name=".MainApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".MainMenu"
                android:exported="true"
                android:label="@string/title_activity_main_menu"
                android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                    android:name="android.app.lib_name"
                    android:value="" />
        </activity>
        <activity android:name=".homeTaskList.HomeTaskListActivity" />
        <activity
                android:name=".shopList.ShopListActivity"
                android:label="@string/title_activity_shop_list" />
        <activity
                android:name=".babyTask.BabyActivity"
                android:label="@string/title_activity_baby" />
        <activity android:name=".MainActivity">

        </activity>
    </application>

</manifest>